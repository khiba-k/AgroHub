import { List, ShoppingCart, Package } from "lucide-react";

interface NavItem {
  label: string;
  icon: React.ElementType;
  href: string;
  roles: string[];
  badge?: string;
  children?: NavItem[];
}

export const agroHubNavItems: NavItem[] = [
  {
    label: "Listings",
    icon: List,
    href: "/listings",
    roles: ["farmer",], // adjust roles as needed
  },
  {
    label: "Cart",
    icon: ShoppingCart,
    href: "/cart",
    roles: ["farmer",], // adjust roles as needed
  },
  {
    label: "Orders",
    icon: Package,
    href: "/orders",
    roles: ["farmer",], // adjust roles as needed
  },
];
